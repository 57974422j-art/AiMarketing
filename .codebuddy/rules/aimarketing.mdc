---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-07-28T00:00:00.000Z
provider: 
---

# AiMarketing 开发规则（2026-07-28 修订）
你正在开发一个全栈营销 SaaS 平台。
技术栈：Next.js 14 + TypeScript + Prisma 5.22.0 + SQLite + Tailwind CSS。
AI 统一入口：src/lib/ai-providers.ts。
数据库：prisma/schema.prisma。
鉴权：JWT Cookie，前端 fetch 必须带 credentials: 'include'。

## 分工（铁律）
- 本地代码修改、git add/commit/push：由 AI 助手完成。
- 服务器操作（git pull / prisma db push / build / pm2 restart / 上传发布文件）：**始终由用户本人执行**，AI 只提供可复制的完整命令。
- 服务器标准部署：cd /root/AiMarketing && git pull origin master && npm run build && pm2 restart aimarketing
  （涉及 schema 改动时在 build 前补：npx prisma db push && npx prisma generate）

## 强制规则
1. 不读 .next、node_modules、.git。
2. 数据库安全：严禁 prisma migrate reset、db push --force-reset、--accept-data-loss。
   本地允许执行 npx prisma generate（只生成 TS 类型，不碰数据库）；本地 db push 只打本地库，与服务器数据无关。
3. 删除文件、修改 package.json/tsconfig.json 前须先说明原因并征得同意
   （例外：npm run release:electron 脚本自动同步 package.json 版本号）。
4. 按功能步骤分批修改：一步可包含多个关联文件，每步完成后汇报改了什么、等确认再进下一步。
   高风险文件（prisma/schema.prisma、electron/main.js）仍单独改、单独确认。
5. 公开路径：/ /login /register /ai-copy /video-edit /ai-agent /dashboard
