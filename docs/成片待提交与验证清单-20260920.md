# 成片 A~D 批：待提交 / 部署 / 验证清单（2026-09-20）

> **为什么要这份文件**：Reasonix 的 shell 在本会话**始终不可用**（`shell startup/child-process check failed; command timed out (> 10s)`），
> 所以 ① 我**无法执行**已授权的 `git add/commit/push`，② **无法做任何运行时验证**（Python/ffmpeg 一行都跑不了）。
> 这份清单让**你自己就能走完**，且即使会话上下文丢失也照做不误。

---

## 一、提交 + 推送（★白名单制，禁止 `git add -A`）

```powershell
cd D:\AiMarketing
git status --short          # 先看清；把输出贴给 AI 也行，让它帮你核对有没有漏/多
```

```powershell
# 本会话碰过的全部文件（对未改动的文件 add 是空操作 → 安全超集）
git add src/app/api/agent/chat/route.ts src/app/agent/page.tsx src/lib/agent/video-material.ts
git add src/app/api/agent/make-video-status/route.ts src/app/api/storage/file/route.ts
git add src/app/api/agent/prefs/route.ts src/app/digital-human/page.tsx src/lib/agent/prompts.ts
git add scripts/video-factory/make.py scripts/video-factory/render.py scripts/video-factory/tts.py
git add EXECUTION_LOG.md PROJECT.md ISSUES.md
git add "docs/成片v2规划-表单化与本地播放-20260920.md"
git add "docs/成片待提交与验证清单-20260920.md"

git status --short          # 确认暂存区只有预期文件
```

```powershell
git commit -m "feat(成片): 表单化(一张卡一次提交)+画幅/时长/素材数联动+Ken Burns+逐字卡点+主色底板+ASS词级字幕+BGM+克隆音色; fix: subtitle误用/播放卡顿/长视频截断/入口词漏判"
git push origin master
```

> ⚠️ **不要提交** `.reasonix/`（Reasonix 本地目录）。建议在编辑器里给根 `.gitignore` 加一行 `.reasonix/`（该文件是 UTF-16，AI 的工具写不了）。

---

## 二、服务器部署

```bash
cd /root/AiMarketing && bash scripts/deploy-server.sh
git log --oneline -1        # 确认是最新提交
```

---

## 三、部署后验证（替 AI 完成它跑不了的那部分）

### ① 确认新代码真的上去了（标记法）

```bash
cd /root/AiMarketing
grep -c "VF_SUBSIZE_V1\|VF_KARAOKE_V1\|VF_BGM_V1\|VF_SYNC_V1\|VF_TINT_V1" scripts/video-factory/render.py   # 期望 ≥5
grep -c "VF_FORM_V1\|VF_VOICE_V1\|VF_SPLIT_V1\|VF_ENTRY_V1" src/app/api/agent/chat/route.ts                 # 期望 ≥4
grep -c "shot_text" scripts/video-factory/render.py                                                          # 期望 ≥1
```
**任何一个 0 → 说明那个文件没 add 上**（历史踩过：`render.py` 掉队导致字幕问题白等一轮）。

### ② 渲染链整条自检（★最关键 —— 覆盖全部新链路）

```bash
python3 scripts/video-factory/render.py --selftest --workdir /tmp/vf --out /tmp/vf/selftest.mp4
```

**期望输出（逐行核对）**：
```
[VF] ffmpeg=/usr/bin/ffmpeg  字体=/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc
[VF] 底板色跟随素材: 0x......          ← C4 主色底板生效
[VF] 第 1 镜 OK  title                 ← 逐字浮现滤镜没报错
[VF] 第 2 镜 OK  bgimage               ← ★新滤镜链（split/模糊铺底/contain overlay/zoompan）
[VF] 第 3 镜 OK  list
[VF] 第 4 镜 OK  number
[VF] 第 5 镜 OK  end
[VF] 拼接完成 -> ...
[VF] 字幕已烧入 -> /tmp/vf/subs.ass (字号 30)   ← ★ASS 卡拉OK + 字号自适应
[VF] ✅ 成片: /tmp/vf/selftest.mp4
```
**任何 `第 N 镜渲染失败(...)` 或 `ASS 生成失败` → 把那一行原样贴给 AI**（这正是它唯一验不到的部分）。

### ③ 走一遍真实成片（表单 + 长视频）

1. 页面发「帮我做一个视频」→ **应直接弹出一张设置表单**（画面来源/画幅/时长/音色/主题/BGM + 🚀 开始出片）
2. 选 **180 秒** → 点 **🎞 素材合成** → 出确认卡 → 确认
3. 出片后看日志（能直接判断每环节是否生效）：

```bash
grep -E "\[表单\]|\[文案\]|\[分镜\]|\[扩写\]|\[素材配比\]|\[入队\]|\[BGM\]" \
  /root/AiMarketing/storage/1/video-factory/vf_debug.log | tail -20
```
**期望**：`[文案] AI 写 7xx 字（目标 810）`、`[分镜] 2x 镜`、`[素材配比] 时长180s → 取图上限 30 张`。

4. 结果核对：

```bash
python3 - <<'PY'
import json, glob, os
f = sorted(glob.glob('/root/AiMarketing/storage/1/video-factory/work_*/storyboard.voiced.json'), key=os.path.getmtime)[-1]
d = json.load(open(f)); s = d['shots']
print('镜数', len(s), '总时长', round(sum(float(x.get('dur',0)) for x in s),1), '秒')
print('抽查第 3 镜 subtitle:', (s[2].get('subtitle') or '')[:60] if len(s)>2 else '-')
print('抽查第 3 镜 text    :', (s[2].get('text') or '')[:20] if len(s)>2 else '-')
PY
```
**判读**：
- `总时长` 应 ≈180（不是 14/50）—— 修的是「`subtitle` 缺失导致配音念大字」
- `subtitle` 应是**文案整句**；`text` 是**4~8 字短语** —— 两者不该相同（原"双字幕"根因）
- 播放不再每 3 秒卡（302 到 OSS，原生 Range）

---

## 四、已知折中 / 待办（**不当作已实现**）

| 项 | 实情 | 要做的话 |
|---|---|---|
| **"叠化转场"** | 实际是**每镜淡入淡出**（`fade=t=in/out` ≤0.2s），**不是 xfade** | 拼接阶段做视频 xfade + 音频等量交叉淡化并重算时间轴；**改完必须实跑** |
| **「📤 我上传素材」** | 表单有入口，但成片**不消费聊天附件** → 已诚实提示"下个版本"并退回表单 | 附件落盘到任务目录 → 当作素材参与排镜 |
| **D1 客户端播本地库** | 播放仍走服务器/OSS | 改 `electron/`（暴露本地路径）→ **必须重打包发版** |
| **"混合 / 全部 AI 生成"** | 表单里是**禁用**状态（灰色），点选会明确回"开发中" | 接逐镜 AI 生成 + 拼接（成本按秒计费） |
| **词级字幕精度** | 按「字数均分镜时长」估算，**不是 funasr 真实字级时间戳** | 服务器装 funasr 后改用它（客户端已有；服务器未装） |
